package com.so0420.eunchoy

import android.app.Application

class EunchoyApp : Application() {
    override fun onCreate() {
        super.onCreate()
        // Notification channels are (re)created here once the notification layer lands.
    }
}
